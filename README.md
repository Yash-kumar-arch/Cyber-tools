# Password Tools

A Python command line tool that checks password strength and generates strong passwords.

## Features
- Checks length, uppercase, lowercase, digits and special characters
- Gives suggestions to improve weak passwords
- Generates strong random passwords

## How to run
python pass_strength_checker.py
